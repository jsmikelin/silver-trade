# Local price updater configuration. Copy this file to .env; .env is ignored by Git.
# Create a key at https://gold-api.com/ and never commit the real value.
GOLD_API_KEY=replace_with_your_gold_api_key
